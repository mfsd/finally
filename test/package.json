{
  "name": "finally-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E tests for the FinAlly integrated app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "typescript": "^5.8.3"
  }
}
